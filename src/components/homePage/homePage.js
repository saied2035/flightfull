import Navbar from '../navbar/navbar';

const HomePage = () => (
  <main className="flex">
    <Navbar />
    <p className="text-3xl font-bold underline">
      Home Page!
    </p>
  </main>
);

export default HomePage;
